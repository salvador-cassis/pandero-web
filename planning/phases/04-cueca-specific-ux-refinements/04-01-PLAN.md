---
phase: 04-cueca-specific-ux-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pandero/player.js
  - pandero/player.css
  - pandero/lora-regular.woff2
autonomous: false
requirements:
  - CUE-01
must_haves:
  truths:
    - "On first load, the BPM display shows 112 BPM (not 111)"
    - "Only one slider exists in the widget: Tempo"
    - "No Tempo label text appears — only the live BPM value"
    - "The widget renders in Lora font, not Caveat"
    - "Font sizes are larger: label 1rem, value 1.25rem"
  artifacts:
    - path: "pandero/lora-regular.woff2"
      provides: "Self-hosted Lora Regular 400 font file"
    - path: "pandero/player.css"
      provides: "Updated font-face, token values"
      contains: "font-family: 'Lora'"
    - path: "pandero/player.js"
      provides: "Simplified DOM with single tempo slider, 112 BPM default"
  key_links:
    - from: "pandero/player.css"
      to: "pandero/lora-regular.woff2"
      via: "@font-face src url"
      pattern: "url\\('./lora-regular\\.woff2'\\)"
    - from: "pandero/player.js"
      to: "#pandero-tempo-val"
      via: "initial display text in sliderRow call"
      pattern: "112 BPM"
---

<objective>
Simplify the widget to a single focused tempo control for cueca musicians: remove pitch and volume sliders, set default tempo to 112 BPM, replace Caveat font with Lora for legibility, and remove the "Tempo" label so only the live BPM value displays.

Purpose: The widget should feel like a single instrument control, not a DAW. Every change reinforces the cueca identity established in Phase 3.
Output: Updated player.js and player.css with simplified DOM and Lora typography.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@planning/PROJECT.md
@planning/ROADMAP.md
@planning/STATE.md
@planning/phases/04-cueca-specific-ux-refinements/04-CONTEXT.md
@pandero/player.js
@pandero/player.css

<interfaces>
<!-- Current player.js DOM creation (lines 39-81) -->
<!-- sliderRow(id, label, min, max, step, value, valId, valText) creates: -->
<!--   div.pandero-slider-row > div.pandero-slider-header > [label, span.pandero-value] + input.pandero-slider -->

<!-- Current player.css tokens (lines 23-34) -->
<!--   --pandero-font: 'Caveat', cursive; -->
<!--   --pandero-size-label: 0.9rem; -->
<!--   --pandero-size-value: 1.1rem; -->

<!-- Current @font-face (lines 11-17) -->
<!--   font-family: 'Caveat'; src: url('./caveat-regular.woff2') -->

<!-- Current buildDOM() slider calls (lines 73-77) -->
<!--   sliderRow('tempo',  'Tempo',   0.5, 1.5, 0.01, 1.0, 'pandero-tempo-val', '111 BPM') -->
<!--   sliderRow('pitch',  'Pitch',  -6,   6,   1,    0,   'pandero-pitch-val', '0 st') -->
<!--   sliderRow('volume', 'Volumen', 0,   1,   0.01, 1.0,  null, null) -->

<!-- Audio functions that reference removed sliders (lines 166-242) -->
<!--   startPlayback() line 167: stNode.parameters.get('pitchSemitones').value = parseInt(...pandero-pitch...) -->
<!--   handlePitchChange() lines 216-221: reads pandero-pitch, writes pitchSemitones -->
<!--   handleVolumeChange() lines 227-230: reads pandero-volume, writes gainNode.gain -->
<!--   Event wiring lines 236-242: pitchSlider, volumeSlider declarations + addEventListener -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Lora font and update CSS typography</name>
  <read_first>
    - pandero/player.css (current @font-face, tokens at lines 11-34)
    - planning/phases/04-cueca-specific-ux-refinements/04-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <files>pandero/lora-regular.woff2, pandero/player.css</files>
  <action>
**Step 1: Download Lora Regular woff2.**

Fetch the current Lora woff2 URL from Google Fonts API (same technique used for Caveat in Phase 3 — the direct URL changes over time):

```bash
curl -s 'https://fonts.googleapis.com/css2?family=Lora:wght@400&display=swap' \
  -H 'User-Agent: Mozilla/5.0' | grep -oP "url\(\K[^)]+\.woff2" | head -1
```

Download the woff2 file from that URL to `pandero/lora-regular.woff2`.

If the grep approach fails, try the alternative: visit `https://fonts.google.com/specimen/Lora` via WebFetch to find the current CDN URL pattern, then curl the woff2 directly.

**Step 2: Update player.css @font-face (lines 11-17).**

Replace the entire `@font-face` block:
```css
@font-face {
  font-family: 'Lora';
  src: url('./lora-regular.woff2') format('woff2');
  font-weight: 400;
  font-style: normal;
  font-display: swap;
}
```

Change the comment above it from "Caveat Regular (handwritten aesthetic)" to "Lora Regular (calligraphic warmth, legible)".

**Step 3: Update CSS custom property tokens (lines 23-34).**

Change these three token values inside the `#pandero-player` rule:
- `--pandero-font: 'Lora', serif;` (was `'Caveat', cursive` — per D-07)
- `--pandero-size-label: 1rem;` (was `0.9rem` — per D-08)
- `--pandero-size-value: 1.25rem;` (was `1.1rem` — per D-08)

All other tokens remain unchanged.

**Step 4: Delete old Caveat font file (discretion — nothing references it after this phase).**

```bash
rm pandero/caveat-regular.woff2
```

**Step 5: Update CSS comments.**

Change the controls container comment (line 91) from "three sliders stacked vertically" to "tempo slider (single control)".
  </action>
  <verify>
    <automated>test -f pandero/lora-regular.woff2 && file pandero/lora-regular.woff2 | grep -q "font" && grep -q "font-family: 'Lora'" pandero/player.css && grep -q "'Lora', serif" pandero/player.css && grep -q "1rem" pandero/player.css && grep -q "1.25rem" pandero/player.css && ! test -f pandero/caveat-regular.woff2 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `pandero/lora-regular.woff2` exists and is a valid font file (file command reports font/woff2)
    - `pandero/player.css` contains `font-family: 'Lora';` in the @font-face block
    - `pandero/player.css` contains `--pandero-font: 'Lora', serif;` in the #pandero-player rule
    - `pandero/player.css` contains `--pandero-size-label: 1rem;`
    - `pandero/player.css` contains `--pandero-size-value: 1.25rem;`
    - `pandero/player.css` @font-face src references `./lora-regular.woff2`
    - `pandero/caveat-regular.woff2` does NOT exist (deleted)
    - No references to 'Caveat' remain in player.css
  </acceptance_criteria>
  <done>Lora font downloaded and self-hosted. CSS tokens updated for Lora family, serif fallback, and increased sizes. Old Caveat file removed.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify player.js — remove pitch/volume, set 112 BPM default, remove Tempo label</name>
  <read_first>
    - pandero/player.js (full file — all 251 lines)
    - planning/phases/04-cueca-specific-ux-refinements/04-CONTEXT.md (D-01 through D-05)
    - CLAUDE.md (tandem pattern, GainNode rules)
  </read_first>
  <files>pandero/player.js</files>
  <action>
**Per D-01: Change default tempo to 112 BPM.**

In `buildDOM()` (line 74), change the `sliderRow('tempo', ...)` call:
- Change default value from `1.0` to `1.009` (ratio = 112/111)
- Change display text from `'111 BPM'` to `'112 BPM'`

The full call becomes:
```js
sliderRow('tempo', 'Tempo', 0.5, 1.5, 0.01, 1.009, 'pandero-tempo-val', '112 BPM')
```

**Per D-02: Remove pitch slider from DOM.**

In `buildDOM()` (line 75), delete the entire line:
```js
sliderRow('pitch',  'Pitch',  -6,   6,   1,    0,   'pandero-pitch-val', '0 st'),
```

**Per D-03: Remove volume slider from DOM.**

In `buildDOM()` (line 76), delete the entire line:
```js
sliderRow('volume', 'Volumen', 0,   1,   0.01, 1.0,  null, null)
```

After removal, the `controls.append(...)` call should contain only the tempo sliderRow.

**Per D-05: Remove the "Tempo" label from the slider row.**

Modify the `sliderRow()` function (lines 39-61) so that when `label` is falsy (null or empty string), no `<label>` element is created. Change the section that creates `lbl` (lines 42-44):

```js
if (label) {
  const lbl = el('label');
  lbl.textContent = label;
  lbl.htmlFor = 'pandero-' + id;
  header.appendChild(lbl);
}
```

Then change the tempo sliderRow call to pass `null` instead of `'Tempo'` for the label parameter:
```js
sliderRow('tempo', null, 0.5, 1.5, 0.01, 1.009, 'pandero-tempo-val', '112 BPM')
```

**Per D-02: Remove pitch-related audio code.**

1. In `startPlayback()` (line 167), delete:
```js
stNode.parameters.get('pitchSemitones').value = parseInt(document.getElementById('pandero-pitch').value);
```

2. Delete the entire `handlePitchChange()` function (lines 216-221).

3. In the event wiring section (lines 236-237), delete the `pitchSlider` declaration:
```js
const pitchSlider = document.getElementById('pandero-pitch');
```
And delete the corresponding event listener (line 241):
```js
pitchSlider.addEventListener('input', handlePitchChange);
```

**Per D-03: Remove volume-related code.**

1. Delete the entire `handleVolumeChange()` function (lines 227-230).

2. In the event wiring section (line 237), delete the `volumeSlider` declaration:
```js
const volumeSlider = document.getElementById('pandero-volume');
```
And delete the corresponding event listener (line 242):
```js
volumeSlider.addEventListener('input', handleVolumeChange);
```

**IMPORTANT: Do NOT remove the GainNode from init() (lines 124-127).** Per CLAUDE.md and D-03, the GainNode stays in the audio graph at fixed gain 1.0. Removing it would require rewiring source -> stNode -> destination.

**Update file header comment** (line 2): Change "Phase 3: Mobile Polish and Embedding" to "Phase 4: Cueca-Specific UX Refinements".
  </action>
  <verify>
    <automated>grep -c "sliderRow" pandero/player.js | grep -q "^1$" && grep -q "1.009" pandero/player.js && grep -q "'112 BPM'" pandero/player.js && ! grep -q "handlePitchChange" pandero/player.js && ! grep -q "handleVolumeChange" pandero/player.js && ! grep -q "pitchSlider" pandero/player.js && ! grep -q "volumeSlider" pandero/player.js && ! grep -q "pandero-pitch" pandero/player.js && ! grep -q "pandero-volume" pandero/player.js && grep -q "gainNode" pandero/player.js && grep -q "sliderRow('tempo', null" pandero/player.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 1 `sliderRow` call exists in player.js (the tempo slider only)
    - The tempo sliderRow call uses value `1.009` and display text `'112 BPM'`
    - The tempo sliderRow call passes `null` as the label parameter (no "Tempo" text)
    - No reference to `handlePitchChange` exists anywhere in the file
    - No reference to `handleVolumeChange` exists anywhere in the file
    - No reference to `pitchSlider` or `volumeSlider` exists anywhere in the file
    - No reference to `pandero-pitch` or `pandero-volume` exists anywhere in the file (no DOM element IDs for removed sliders)
    - `gainNode` still appears in the file (GainNode preserved in audio graph per CLAUDE.md)
    - `startPlayback()` still contains the tandem pattern: `source.playbackRate.value = ratio` and `stNode.parameters.get('playbackRate').value = ratio`
    - `startPlayback()` does NOT contain `pitchSemitones`
    - The `sliderRow` function conditionally creates the label element (only when label is truthy)
  </acceptance_criteria>
  <done>Widget has exactly one slider (tempo) at 112 BPM default with no label text. Pitch and volume controls fully removed from DOM, event wiring, and audio code. GainNode and tandem pattern preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify simplified widget in browser</name>
  <files>pandero/player.js, pandero/player.css</files>
  <action>
Human verifies the simplified widget visually and functionally in a browser. No code changes in this task.
  </action>
  <what-built>
    Simplified pandero widget: single tempo slider at 112 BPM, Lora font, no pitch/volume controls, no "Tempo" label.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npx serve .` from project root
    2. Open `http://localhost:3000/pandero/poc.html` (or the host page with the embed)
    3. Verify visual:
       - The widget renders in Lora font (serif, not handwritten Caveat)
       - Only ONE slider is visible (tempo) — no pitch or volume sliders
       - The slider header shows only "112 BPM" — no "Tempo" label text to the left
       - Font sizes appear slightly larger than Phase 3
    4. Verify interaction:
       - Click the hexagon play button — audio plays
       - Drag the tempo slider — BPM display updates live, playback speed changes
       - Press Space — toggles play/pause
    5. Open DevTools console — no errors
  </how-to-verify>
  <verify>Human confirms visual and functional correctness in browser</verify>
  <done>User approves the simplified widget appearance and behavior</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pandero/lora-regular.woff2` exists and is a valid woff2 font file
- `pandero/player.css` references Lora (not Caveat) with increased size tokens
- `pandero/player.js` has exactly 1 sliderRow call (tempo only) with default 1.009 / "112 BPM"
- No pitch or volume DOM elements, handlers, or event listeners remain in player.js
- GainNode and tandem playbackRate pattern preserved in audio graph
- Widget loads and plays correctly in browser
</verification>

<success_criteria>
CUE-01 satisfied: Default tempo is 112 BPM on first load.
Widget simplified to single focused control per user decisions D-01 through D-08.
Typography upgraded from Caveat to Lora with increased sizes for legibility.
Audio engine untouched — GainNode, tandem pattern, all Phase 1-3 behavior preserved.
</success_criteria>

<output>
After completion, create `planning/phases/04-cueca-specific-ux-refinements/04-01-SUMMARY.md`
</output>
